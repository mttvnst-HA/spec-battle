node_modules
dist
.vite
balance-report.json
.tuning-abort
tuning-summary.md
balance-baseline.next.json

# Dialog authoring scratch
scratch/

# Raw reference PDFs (source inputs for docs/dialog-source-material.md;
# keep local-only, the distilled doc is the tracked artifact)
reference/*.pdf
